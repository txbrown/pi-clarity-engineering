---
description: Apply Clarity Engineering Plan mode
argument-hint: "[ticket, request, or shaped context]"
---

Use the `cl-plan` skill to process the following input:

<input>
$ARGUMENTS
</input>

Follow Clarity Engineering: `Shape → Plan → Build → Review → Compound`. Resolve the intent source first. Plan = Slice + Specify. Slice only when useful; if the source ticket is small enough, say no child tickets are needed. Materialize buildable slices in the configured issue tracker when useful and safe. Escalate before tracker mutations unless pre-authorized. Specify acceptance details for the selected next slice/ticket. Proceed autonomously; escalate only when a trigger fires. Ask one focused question per escalation.
