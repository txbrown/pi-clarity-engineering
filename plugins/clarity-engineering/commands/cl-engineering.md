---
description: Route work through Clarity Engineering
argument-hint: "[request, ticket, plan, diff, or context]"
---

Use the `cl-engineering` skill to process the following input:

<input>
$ARGUMENTS
</input>

Follow Clarity Engineering: `Shape → Plan → Build → Review → Compound`. Route this request. Resolve intent source, retrieve narrow context, perform stage work. For Build, classify depth. Proceed autonomously; escalate only when a trigger fires. In Pi, use the TUI `ask_user` tool when available.
